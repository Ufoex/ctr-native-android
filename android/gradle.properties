# The project depends on androidx.appcompat, which AGP refuses to resolve
# without this flag, so the Android build could not be assembled at all.
android.useAndroidX=true
android.nonTransitiveRClass=true

org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
